#include <bits/stdc++.h>
using namespace std;

int main() {
        int w;
        cin >> w;
        if(w % 2 == 0 && w > 2) {
                puts("YES");
        } else {
                puts("NO");
        }
        return 0;
}
